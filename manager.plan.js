/* The plan manager
 */
 
module.exports =
{
	run: function(room)
	{
		// Find room elements
		var creeps = room.find(FIND_MY_CREEPS);

		var tickMap = [];
		
		for (var key in creeps)
		{
			
		}
	}
};